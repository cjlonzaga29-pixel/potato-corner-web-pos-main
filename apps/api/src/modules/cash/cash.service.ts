import { cashRepository } from './cash.repository.js';

/**
 * Cash business logic. Called by the router after Zod validation;
 * never calls Prisma directly — always goes through cashRepository.
 */
export const cashService = {
  // TODO(Phase 1+): implement business logic for the cash module.
};

void cashRepository;
