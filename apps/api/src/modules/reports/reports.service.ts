import { reportsRepository } from './reports.repository.js';

/**
 * Reports business logic. Called by the router after Zod validation;
 * never calls Prisma directly — always goes through reportsRepository.
 */
export const reportsService = {
  // TODO(Phase 1+): implement business logic for the reports module.
};

void reportsRepository;
