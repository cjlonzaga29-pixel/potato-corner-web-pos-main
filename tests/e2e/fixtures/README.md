Test data and authenticated-session storage state files go here (e.g. `admin.auth.json`, `supervisor.auth.json`, `staff.auth.json`), generated by a Playwright global setup once Phase 1 authentication exists.
