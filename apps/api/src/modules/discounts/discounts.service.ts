import { discountsRepository } from './discounts.repository.js';

/**
 * Discounts business logic. Called by the router after Zod validation;
 * never calls Prisma directly — always goes through discountsRepository.
 */
export const discountsService = {
  // TODO(Phase 1+): implement business logic for the discounts module.
};

void discountsRepository;
