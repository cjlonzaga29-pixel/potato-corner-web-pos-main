'use client';

import { InventoryTransferForm } from '@/components/branch-ops/inventory-transfer-form';

export default function TransferPage() {
  return <InventoryTransferForm basePath="/supervisor" />;
}
