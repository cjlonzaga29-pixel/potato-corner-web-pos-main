import { test } from '@playwright/test';

// TODO(Phase 9, 11): denomination cash counts, variance calculation,
// supervisor variance approval, and cashier handover detection.
test.skip('cash count and variance approval workflow', async () => {});
